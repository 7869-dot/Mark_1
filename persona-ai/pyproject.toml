[project]
name = "persona-ai"
version = "0.1.0"
description = "Persona AI API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
